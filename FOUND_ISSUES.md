0. Implement cleanup for task - [DONE] "Clean up" button on the task (any non-running state): stops BE/FE, kills the task tmux session, runs the env's new "Cleanup command" (Environments → Configure → Services) from the worktree, removes the worktree + local branch, drops the task's logs. Refuses when the worktree holds unpushed commits / uncommitted changes (offers "clean up anyway"). The task, its artifacts and history stay; "Delete" (also new in the UI) removes everything. API: POST /api/tasks/:id/cleanup[?force=1].
 a. Stop be/fe - probably needs a custom command, same as for start - [DONE] env.cleanup_command
 b. clean worktree - [DONE]
 c. anything other that needs cleaning - [DONE] tmux session, logs; pushed commits / PR untouched


1. There is no section in design proposal that simply explain the change - in simple short words, information dense no flood. Should be added as Proposed changes - [DONE] design.md now has "## 4. Proposed changes" (2–6 plain bullets, ≤120 words, no table) before the Change table; enforced by the contract retry; shown on the Work-tab design card and as the default Design subtab. Existing proposals predate it.
2. If I accidentally click  Approve instead of Request changes in design phase (or actually any other) - I now need to restart it to request changes. No sure how to address, would be good to have a simple button like "Return to <phase> and add comments" - [DONE] "Return to a stage…" button (any non-running state): pick Design Proposal / Implementation / User Review / PR Creation Review ≤ current stage, add notes (draft line comments go along to Implementation); the stage re-runs with them as reviewer notes and later stages follow. If a PR already exists, the PR step just pushes instead of creating a second PR.
3. Push notifications, when clicked - open some random Finder window instead of browser with correct path - [DONE] desktop notifications now go through terminal-notifier (installed via brew) with -open → the task in the local UI (Task managers → Notifications → "URL of this UI on this Mac", default http://localhost:5173). osascript's display notification has no click action (that was the Finder/Script Editor window).
4. All links in the App should be opened in New Tab instead of Current Tab - [DONE] every http(s)/artifact link opens in a new tab (markdown links via a DOMPurify hook, everything else via a document-level click handler); in-app #/ links stay.
5. On mobile, cannot open task view - shows blank screen - [PARTIAL] could not reproduce in Chromium at 390×844 (dev + built bundle); no WebKit here. Fixed the likely cause (localStorage writes throw in Safari private mode / storage-restricted → React blanked the tree) with a never-throwing storage wrapper, made xterm lazy-loaded everywhere, and added an error boundary + a bottom error line: a crash now shows its message and a "Reload" button instead of a blank page. If it happens again, the message on screen says what broke.
6. Remove limit for 3 QA tests - [DONE]
7. In analytics tab + task's cost tab - for non-enterprize account show %of limit spend, besides money and token - [DONE] Stagehand learns how many estimated dollars one 5-hour / 7-day window holds from how far each run moves the account's utilisation (rolling average, backfilled from existing run logs: personal ≈ $54 per 5 h, ≈ $336 per 7 d). Analytics "By AI account" has a "5h / 7d window" column; the task Cost tab has a per-account "window share" tile and a per-run 5h column. Caveat: your own interactive Claude use moves the same windows, which makes the % slightly high.
8. Add bulk task dispatch - [DONE] New task form: tick tickets from the sprint list and/or paste several ids/links (one per line), then choose
  a. Each ticket in it's own PR - Task created per ticket - [DONE] "One task per ticket"
  b. All tickets in 1 (2 if fe + be) PRs - single Task per batch - [DONE] "One task for all": one task, all tickets fetched and given to every stage as one brief, one branch, one PR per repository (title lists every id); Ticket tab shows them all.
9. In task creation - additional field to add extra info for task execution - [DONE] "Extra instructions for the agents" at creation, editable later on the Ticket tab ("Your instructions"); injected into research, design, QA, implementation, PR draft and PR-fix prompts.
10. Account/login model is not account-flexible: log in once per Claude account, have it auto-plug in wherever needed and auto-switch on rate limits. Today each AI account needs its own separate browser login per Stagehand-owned dir (~/.stagehand/browser/<account>), discovered by hitting a block, not surfaced up front — a hacky, not-yet-solid construction. - [PARTIAL] the "discovered by hitting a block" part: GET /api/readiness computes per env who can run agent stages and who can drive Chrome; the Dashboard shows a "Not ready" card and the New task form warns before you start; browser logins are re-checked every 15 min (claude auth status, no agent) so a lost login shows up on the AI accounts page before QA hits it. Rate-limit switching already follows the env's account order for both token and browser stages. Not changed: the browser login still lives per account in ~/.stagehand/browser/<account> (Claude Code keys browser logins by config dir and gives token sessions no Chrome bridge — Stagehand can't copy a login between dirs without touching credentials). A single "log in once" flow needs Claude Code itself to allow the bridge under a token; until then this is the least bad shape.
11. Limits shown on top for non-enterprize account are not updated - my window has copmlemted, I'm on 6% usage it still shows 90% - [DONE] a window whose reset instant has passed is shown as "?" (stale, dimmed) instead of the old number; ↻ on the gauge (and "Refresh limits" on the account card) runs one tiny Sonnet turn that reports the real utilisation; the server also auto-refreshes an expired, idle window once an hour. Hover the gauge for "as of <time>".
12. Dashboard should also show running tasks and tasks that are waiting for approval - [DONE] per env: needs-you rows (incl. waiting for your approval), then "Running" (running/queued, with the live status line), then "Waiting on GitHub / reviewers" (PR stages).
